jwt:
  secret: "ChangeThisSecretKeyForProduction1234"
  expiration: 86400000 # 24 hours in milliseconds
spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
